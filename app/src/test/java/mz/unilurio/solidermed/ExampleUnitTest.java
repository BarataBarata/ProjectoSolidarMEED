package mz.unilurio.solidermed;

import org.junit.*;

import static org.junit.Assert.assertEquals;


/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        //Calendar calendar = Calendar.getInstance();
        //Measures measures=new Measures(calendar.getTime(),3);
        //   System.out.println(" hora inicial "+ measures.getInitialHour());
        //System.out.println(" hora final "+ measures.getPredictedExpulsionHour());

//        ServicoDeParto servicoDeParto=new ServicoDeParto();
        //System.out.println(DataManager.getInstance().getListServico());
        //System.out.println(DataManager.getInstance().getListServico());
        assertEquals(6,4+2);
    }


}